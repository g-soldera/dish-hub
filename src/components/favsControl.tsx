// import { useLocalStorage } from "@/hooks/useLocalStorage"

// export function FavsControl() {
//   const { value } = useLocalStorage("favs")

//   return (
//     <div>
//       {value && value.length > 0 && (
//         <div>
//           {value.map((item: string, index: number) => (
//             <div key={index}>{item}</div>
//           ))}
//         </div>
//       )}
//     </div>
//   )
// }
